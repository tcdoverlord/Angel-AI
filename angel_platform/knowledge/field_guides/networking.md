# Angel Knowledge Field Guide: Networking

This field guide is operational reference material for Angel. It emphasizes observable evidence, reversible changes, least privilege, and explicit user approval before impactful actions.

## 0001. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-1 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0002. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-2 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0003. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-3 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0004. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-4 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0005. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-5 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0006. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-6 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0007. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-7 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0008. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-8 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0009. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-9 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0010. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-10 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0011. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-11 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0012. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-12 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0013. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-13 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0014. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-14 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0015. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-15 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0016. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-16 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0017. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-17 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0018. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-18 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0019. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-19 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0020. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-20 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0021. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-21 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0022. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-22 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0023. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-23 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0024. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-24 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0025. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-25 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0026. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-26 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0027. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-27 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0028. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-28 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0029. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-29 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0030. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-30 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0031. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-31 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0032. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-32 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0033. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-33 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0034. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-34 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0035. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-35 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0036. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-36 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0037. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-37 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0038. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-38 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0039. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-39 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0040. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-40 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0041. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-41 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0042. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-42 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0043. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-43 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0044. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-44 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0045. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-45 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0046. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-46 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0047. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-47 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0048. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-48 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0049. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-49 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0050. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-50 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0051. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-51 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0052. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-52 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0053. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-53 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0054. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-54 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0055. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-55 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0056. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-56 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0057. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-57 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0058. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-58 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0059. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-59 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0060. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-60 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0061. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-61 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0062. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-62 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0063. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-63 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0064. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-64 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0065. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-65 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0066. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-66 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0067. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-67 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0068. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-68 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0069. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-69 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0070. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-70 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0071. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-71 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0072. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-72 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0073. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-73 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0074. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-74 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0075. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-75 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0076. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-76 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0077. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-77 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0078. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-78 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0079. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-79 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0080. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-80 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0081. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-81 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0082. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-82 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0083. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-83 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0084. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-84 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0085. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-85 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0086. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-86 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0087. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-87 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0088. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-88 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0089. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-89 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0090. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-90 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0091. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-91 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0092. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-92 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0093. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-93 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0094. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-94 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0095. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-95 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0096. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-96 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0097. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-97 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0098. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-98 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0099. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-99 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0100. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-100 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0101. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-101 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0102. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-102 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0103. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-103 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0104. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-104 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0105. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-105 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0106. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-106 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0107. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-107 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0108. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-108 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0109. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-109 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0110. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-110 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0111. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-111 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0112. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-112 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0113. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-113 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0114. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-114 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0115. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-115 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0116. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-116 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0117. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-117 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0118. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-118 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0119. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-119 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0120. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-120 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0121. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-121 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0122. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-122 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0123. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-123 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0124. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-124 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0125. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-125 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0126. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-126 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0127. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-127 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0128. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-128 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0129. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-129 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0130. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-130 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0131. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-131 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0132. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-132 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0133. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-133 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0134. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-134 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0135. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-135 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0136. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-136 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0137. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-137 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0138. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-138 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0139. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-139 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0140. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-140 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0141. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-141 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0142. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-142 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0143. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-143 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0144. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-144 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0145. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-145 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0146. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-146 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0147. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-147 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0148. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-148 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0149. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-149 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0150. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-150 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0151. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-151 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0152. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-152 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0153. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-153 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0154. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-154 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0155. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-155 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0156. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-156 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0157. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-157 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0158. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-158 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0159. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-159 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0160. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-160 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0161. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-161 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0162. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-162 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0163. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-163 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0164. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-164 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0165. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-165 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0166. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-166 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0167. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-167 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0168. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-168 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0169. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-169 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0170. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-170 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0171. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-171 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0172. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-172 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0173. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-173 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0174. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-174 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0175. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-175 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0176. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-176 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0177. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-177 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0178. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-178 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0179. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-179 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0180. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-180 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0181. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-181 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0182. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-182 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0183. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-183 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0184. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-184 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0185. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-185 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0186. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-186 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0187. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-187 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0188. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-188 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0189. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-189 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0190. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-190 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0191. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-191 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0192. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-192 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0193. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-193 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0194. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-194 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0195. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-195 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0196. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-196 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0197. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-197 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0198. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-198 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0199. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-199 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0200. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-200 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0201. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-201 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0202. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-202 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0203. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-203 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0204. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-204 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0205. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-205 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0206. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-206 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0207. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-207 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0208. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-208 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0209. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-209 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0210. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-210 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0211. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-211 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0212. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-212 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0213. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-213 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0214. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-214 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0215. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-215 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0216. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-216 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0217. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-217 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0218. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-218 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0219. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-219 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0220. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-220 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0221. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-221 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0222. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-222 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0223. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-223 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0224. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-224 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0225. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-225 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0226. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-226 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0227. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-227 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0228. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-228 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0229. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-229 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0230. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-230 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0231. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-231 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0232. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-232 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0233. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-233 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0234. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-234 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0235. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-235 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0236. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-236 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0237. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-237 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0238. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-238 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0239. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-239 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0240. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-240 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0241. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-241 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0242. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-242 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0243. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-243 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0244. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-244 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0245. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-245 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0246. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-246 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0247. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-247 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0248. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-248 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0249. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-249 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0250. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-250 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0251. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-251 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0252. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-252 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0253. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-253 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0254. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-254 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0255. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-255 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0256. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-256 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0257. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-257 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0258. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-258 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0259. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-259 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0260. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-260 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0261. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-261 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0262. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-262 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0263. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-263 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0264. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-264 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0265. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-265 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0266. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-266 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0267. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-267 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0268. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-268 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0269. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-269 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0270. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-270 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0271. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-271 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0272. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-272 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0273. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-273 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0274. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-274 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0275. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-275 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0276. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-276 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0277. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-277 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0278. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-278 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0279. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-279 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0280. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-280 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0281. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-281 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0282. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-282 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0283. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-283 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0284. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-284 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0285. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-285 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0286. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-286 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0287. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-287 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0288. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-288 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0289. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-289 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0290. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-290 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0291. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-291 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0292. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-292 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0293. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-293 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0294. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-294 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0295. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-295 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0296. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-296 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0297. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-297 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0298. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-298 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0299. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-299 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0300. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-300 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0301. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-301 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0302. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-302 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0303. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-303 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0304. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-304 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0305. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-305 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0306. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-306 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0307. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-307 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0308. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-308 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0309. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-309 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0310. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-310 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0311. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-311 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0312. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-312 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0313. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-313 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0314. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-314 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0315. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-315 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0316. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-316 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0317. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-317 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0318. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-318 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0319. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-319 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0320. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-320 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0321. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-321 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0322. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-322 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0323. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-323 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0324. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-324 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0325. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-325 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0326. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-326 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0327. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-327 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0328. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-328 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0329. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-329 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0330. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-330 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0331. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-331 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0332. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-332 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0333. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-333 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0334. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-334 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0335. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-335 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0336. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-336 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0337. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-337 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0338. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-338 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0339. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-339 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0340. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-340 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0341. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-341 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0342. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-342 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0343. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-343 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0344. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-344 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0345. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-345 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0346. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-346 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0347. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-347 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0348. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-348 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0349. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-349 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0350. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-350 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0351. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-351 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0352. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-352 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0353. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-353 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0354. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-354 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0355. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-355 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0356. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-356 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0357. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-357 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0358. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-358 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0359. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-359 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0360. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-360 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0361. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-361 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0362. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-362 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0363. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-363 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0364. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-364 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0365. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-365 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0366. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-366 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0367. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-367 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0368. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-368 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0369. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-369 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0370. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-370 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0371. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-371 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0372. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-372 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0373. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-373 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0374. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-374 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0375. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-375 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0376. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-376 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0377. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-377 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0378. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-378 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0379. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-379 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0380. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-380 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0381. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-381 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0382. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-382 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0383. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-383 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0384. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-384 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0385. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-385 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0386. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-386 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0387. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-387 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0388. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-388 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0389. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-389 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0390. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-390 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0391. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-391 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0392. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-392 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0393. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-393 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0394. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-394 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0395. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-395 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0396. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-396 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0397. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-397 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0398. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-398 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0399. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-399 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0400. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-400 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0401. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-401 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0402. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-402 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0403. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-403 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0404. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-404 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0405. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-405 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0406. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-406 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0407. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-407 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0408. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-408 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0409. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-409 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0410. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-410 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0411. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-411 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0412. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-412 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0413. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-413 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0414. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-414 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0415. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-415 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0416. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-416 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0417. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-417 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0418. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-418 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0419. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-419 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0420. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-420 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0421. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-421 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0422. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-422 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0423. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-423 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0424. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-424 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0425. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-425 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0426. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-426 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0427. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-427 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0428. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-428 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0429. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-429 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0430. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-430 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0431. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-431 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0432. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-432 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0433. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-433 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0434. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-434 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0435. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-435 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0436. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-436 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0437. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-437 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0438. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-438 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0439. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-439 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0440. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-440 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0441. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-441 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0442. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-442 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0443. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-443 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0444. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-444 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0445. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-445 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0446. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-446 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0447. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-447 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0448. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-448 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0449. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-449 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0450. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-450 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0451. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-451 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0452. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-452 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0453. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-453 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0454. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-454 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0455. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-455 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0456. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-456 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0457. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-457 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0458. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-458 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0459. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-459 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0460. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-460 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0461. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-461 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0462. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-462 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0463. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-463 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0464. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-464 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0465. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-465 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0466. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-466 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0467. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-467 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0468. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-468 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0469. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-469 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0470. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-470 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0471. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-471 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0472. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-472 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0473. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-473 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0474. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-474 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0475. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-475 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0476. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-476 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0477. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-477 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0478. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-478 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0479. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-479 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0480. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-480 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0481. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-481 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0482. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-482 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0483. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-483 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0484. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-484 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0485. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-485 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0486. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-486 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0487. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-487 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0488. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-488 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0489. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-489 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0490. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-490 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0491. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-491 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0492. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-492 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0493. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-493 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0494. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-494 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0495. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-495 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0496. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-496 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0497. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-497 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0498. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-498 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0499. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-499 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0500. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-500 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0501. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-501 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0502. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-502 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0503. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-503 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0504. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-504 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0505. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-505 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0506. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-506 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0507. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-507 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0508. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-508 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0509. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-509 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0510. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-510 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0511. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-511 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0512. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-512 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0513. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-513 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0514. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-514 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0515. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-515 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0516. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-516 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0517. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-517 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0518. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-518 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0519. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-519 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0520. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-520 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0521. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-521 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0522. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-522 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0523. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-523 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0524. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-524 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0525. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-525 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0526. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-526 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0527. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-527 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0528. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-528 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0529. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-529 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0530. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-530 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0531. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-531 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0532. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-532 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0533. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-533 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0534. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-534 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0535. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-535 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0536. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-536 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0537. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-537 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0538. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-538 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0539. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-539 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0540. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-540 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0541. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-541 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0542. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-542 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0543. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-543 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0544. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-544 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0545. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-545 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0546. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-546 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0547. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-547 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0548. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-548 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0549. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-549 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0550. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-550 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0551. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-551 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0552. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-552 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0553. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-553 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0554. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-554 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0555. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-555 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0556. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-556 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0557. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-557 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0558. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-558 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0559. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-559 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0560. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-560 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0561. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-561 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0562. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-562 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0563. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-563 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0564. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-564 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0565. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-565 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0566. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-566 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0567. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-567 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0568. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-568 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0569. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-569 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0570. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-570 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0571. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-571 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0572. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-572 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0573. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-573 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0574. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-574 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0575. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-575 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0576. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-576 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0577. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-577 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0578. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-578 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0579. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-579 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0580. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-580 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0581. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-581 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0582. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-582 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0583. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-583 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0584. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-584 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0585. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-585 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0586. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-586 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0587. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-587 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0588. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-588 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0589. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-589 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0590. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-590 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0591. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-591 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0592. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-592 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0593. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-593 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0594. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-594 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0595. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-595 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0596. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-596 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0597. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-597 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0598. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-598 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0599. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-599 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0600. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-600 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0601. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-601 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0602. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-602 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0603. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-603 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0604. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-604 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0605. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-605 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0606. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-606 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0607. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-607 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0608. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-608 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0609. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-609 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0610. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-610 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0611. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-611 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0612. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-612 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0613. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-613 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0614. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-614 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0615. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-615 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0616. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-616 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0617. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-617 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0618. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-618 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0619. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-619 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0620. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-620 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0621. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-621 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0622. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-622 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0623. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-623 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0624. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-624 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0625. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-625 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0626. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-626 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0627. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-627 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0628. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-628 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0629. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-629 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0630. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-630 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0631. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-631 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0632. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-632 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0633. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-633 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0634. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-634 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0635. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-635 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0636. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-636 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0637. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-637 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0638. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-638 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0639. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-639 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0640. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-640 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0641. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-641 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0642. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-642 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0643. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-643 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0644. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-644 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0645. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-645 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0646. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-646 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0647. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-647 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0648. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-648 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0649. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-649 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0650. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-650 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0651. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-651 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0652. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-652 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0653. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-653 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0654. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-654 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0655. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-655 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0656. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-656 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0657. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-657 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0658. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-658 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0659. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-659 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0660. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-660 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0661. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-661 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0662. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-662 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0663. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-663 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0664. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-664 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0665. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-665 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0666. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-666 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0667. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-667 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0668. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-668 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0669. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-669 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0670. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-670 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0671. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-671 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0672. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-672 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0673. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-673 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0674. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-674 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0675. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-675 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0676. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-676 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0677. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-677 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0678. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-678 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0679. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-679 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0680. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-680 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0681. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-681 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0682. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-682 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0683. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-683 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0684. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-684 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0685. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-685 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0686. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-686 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0687. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-687 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0688. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-688 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0689. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-689 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0690. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-690 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0691. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-691 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0692. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-692 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0693. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-693 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0694. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-694 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0695. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-695 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0696. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-696 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0697. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-697 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0698. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-698 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0699. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-699 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0700. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-700 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0701. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-701 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0702. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-702 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0703. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-703 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0704. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-704 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0705. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-705 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0706. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-706 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0707. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-707 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0708. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-708 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0709. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-709 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0710. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-710 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0711. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-711 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0712. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-712 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0713. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-713 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0714. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-714 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0715. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-715 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0716. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-716 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0717. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-717 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0718. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-718 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0719. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-719 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0720. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-720 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0721. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-721 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0722. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-722 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0723. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-723 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0724. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-724 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0725. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-725 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0726. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-726 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0727. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-727 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0728. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-728 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0729. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-729 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0730. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-730 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0731. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-731 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0732. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-732 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0733. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-733 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0734. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-734 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0735. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-735 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0736. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-736 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0737. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-737 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0738. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-738 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0739. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-739 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0740. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-740 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0741. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-741 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0742. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-742 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0743. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-743 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0744. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-744 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0745. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-745 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0746. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-746 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0747. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-747 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0748. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-748 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0749. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-749 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0750. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-750 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0751. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-751 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0752. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-752 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0753. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-753 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0754. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-754 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0755. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-755 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0756. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-756 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0757. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-757 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0758. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-758 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0759. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-759 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0760. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-760 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0761. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-761 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0762. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-762 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0763. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-763 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0764. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-764 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0765. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-765 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0766. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-766 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0767. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-767 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0768. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-768 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0769. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-769 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0770. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-770 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0771. Firewalls — Inspect
**Objective:** inspect the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-771 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0772. Tcp — Document
**Objective:** document the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-772 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0773. Tls — Test
**Objective:** test the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-773 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0774. Dns — Compare
**Objective:** compare the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-774 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0775. Dhcp — Isolate
**Objective:** isolate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-775 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0776. Routing — Record
**Objective:** record the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-776 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0777. Firewalls — Validate
**Objective:** validate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-777 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0778. Tcp — Review
**Objective:** review the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-778 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0779. Tls — Restore
**Objective:** restore the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-779 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0780. Dns — Verify
**Objective:** verify the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-780 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0781. Dhcp — Inspect
**Objective:** inspect the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-781 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0782. Routing — Document
**Objective:** document the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-782 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0783. Firewalls — Test
**Objective:** test the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-783 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0784. Tcp — Compare
**Objective:** compare the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-784 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0785. Tls — Isolate
**Objective:** isolate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-785 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0786. Dns — Record
**Objective:** record the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-786 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0787. Dhcp — Validate
**Objective:** validate the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-787 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0788. Routing — Review
**Objective:** review the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-788 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0789. Firewalls — Restore
**Objective:** restore the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-789 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0790. Tcp — Verify
**Objective:** verify the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-790 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0791. Tls — Inspect
**Objective:** inspect the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-791 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0792. Dns — Document
**Objective:** document the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-792 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0793. Dhcp — Test
**Objective:** test the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-793 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0794. Routing — Compare
**Objective:** compare the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-794 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

## 0795. Firewalls — Isolate
**Objective:** isolate the firewalls behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of firewalls. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If firewalls behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-795 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TCP and TLS.

## 0796. Tcp — Record
**Objective:** record the TCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-796 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include TLS and DNS.

## 0797. Tls — Validate
**Objective:** validate the TLS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of TLS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If TLS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-797 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DNS and DHCP.

## 0798. Dns — Review
**Objective:** review the DNS behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DNS. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DNS behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-798 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include DHCP and routing.

## 0799. Dhcp — Restore
**Objective:** restore the DHCP behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of DHCP. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If DHCP behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-799 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include routing and firewalls.

## 0800. Routing — Verify
**Objective:** verify the routing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of routing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If routing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 3-800 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include firewalls and TCP.

