# Angel Knowledge Field Guide: Raspberry Pi

This field guide is operational reference material for Angel. It emphasizes observable evidence, reversible changes, least privilege, and explicit user approval before impactful actions.

## 0001. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-1 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0002. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-2 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0003. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-3 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0004. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-4 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0005. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-5 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0006. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-6 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0007. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-7 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0008. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-8 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0009. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-9 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0010. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-10 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0011. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-11 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0012. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-12 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0013. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-13 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0014. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-14 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0015. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-15 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0016. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-16 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0017. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-17 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0018. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-18 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0019. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-19 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0020. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-20 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0021. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-21 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0022. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-22 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0023. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-23 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0024. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-24 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0025. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-25 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0026. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-26 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0027. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-27 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0028. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-28 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0029. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-29 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0030. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-30 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0031. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-31 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0032. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-32 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0033. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-33 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0034. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-34 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0035. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-35 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0036. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-36 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0037. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-37 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0038. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-38 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0039. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-39 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0040. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-40 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0041. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-41 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0042. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-42 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0043. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-43 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0044. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-44 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0045. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-45 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0046. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-46 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0047. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-47 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0048. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-48 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0049. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-49 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0050. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-50 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0051. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-51 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0052. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-52 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0053. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-53 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0054. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-54 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0055. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-55 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0056. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-56 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0057. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-57 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0058. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-58 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0059. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-59 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0060. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-60 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0061. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-61 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0062. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-62 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0063. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-63 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0064. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-64 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0065. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-65 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0066. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-66 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0067. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-67 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0068. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-68 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0069. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-69 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0070. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-70 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0071. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-71 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0072. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-72 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0073. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-73 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0074. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-74 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0075. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-75 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0076. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-76 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0077. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-77 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0078. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-78 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0079. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-79 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0080. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-80 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0081. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-81 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0082. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-82 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0083. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-83 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0084. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-84 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0085. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-85 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0086. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-86 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0087. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-87 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0088. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-88 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0089. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-89 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0090. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-90 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0091. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-91 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0092. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-92 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0093. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-93 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0094. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-94 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0095. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-95 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0096. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-96 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0097. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-97 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0098. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-98 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0099. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-99 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0100. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-100 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0101. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-101 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0102. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-102 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0103. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-103 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0104. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-104 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0105. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-105 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0106. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-106 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0107. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-107 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0108. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-108 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0109. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-109 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0110. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-110 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0111. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-111 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0112. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-112 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0113. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-113 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0114. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-114 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0115. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-115 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0116. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-116 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0117. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-117 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0118. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-118 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0119. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-119 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0120. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-120 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0121. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-121 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0122. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-122 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0123. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-123 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0124. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-124 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0125. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-125 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0126. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-126 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0127. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-127 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0128. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-128 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0129. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-129 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0130. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-130 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0131. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-131 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0132. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-132 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0133. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-133 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0134. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-134 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0135. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-135 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0136. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-136 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0137. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-137 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0138. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-138 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0139. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-139 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0140. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-140 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0141. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-141 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0142. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-142 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0143. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-143 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0144. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-144 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0145. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-145 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0146. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-146 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0147. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-147 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0148. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-148 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0149. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-149 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0150. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-150 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0151. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-151 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0152. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-152 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0153. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-153 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0154. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-154 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0155. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-155 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0156. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-156 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0157. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-157 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0158. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-158 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0159. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-159 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0160. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-160 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0161. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-161 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0162. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-162 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0163. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-163 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0164. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-164 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0165. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-165 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0166. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-166 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0167. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-167 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0168. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-168 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0169. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-169 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0170. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-170 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0171. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-171 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0172. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-172 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0173. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-173 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0174. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-174 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0175. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-175 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0176. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-176 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0177. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-177 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0178. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-178 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0179. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-179 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0180. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-180 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0181. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-181 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0182. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-182 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0183. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-183 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0184. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-184 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0185. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-185 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0186. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-186 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0187. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-187 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0188. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-188 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0189. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-189 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0190. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-190 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0191. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-191 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0192. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-192 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0193. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-193 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0194. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-194 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0195. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-195 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0196. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-196 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0197. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-197 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0198. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-198 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0199. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-199 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0200. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-200 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0201. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-201 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0202. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-202 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0203. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-203 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0204. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-204 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0205. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-205 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0206. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-206 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0207. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-207 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0208. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-208 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0209. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-209 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0210. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-210 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0211. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-211 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0212. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-212 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0213. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-213 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0214. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-214 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0215. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-215 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0216. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-216 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0217. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-217 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0218. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-218 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0219. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-219 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0220. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-220 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0221. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-221 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0222. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-222 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0223. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-223 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0224. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-224 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0225. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-225 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0226. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-226 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0227. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-227 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0228. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-228 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0229. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-229 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0230. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-230 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0231. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-231 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0232. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-232 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0233. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-233 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0234. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-234 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0235. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-235 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0236. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-236 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0237. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-237 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0238. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-238 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0239. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-239 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0240. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-240 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0241. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-241 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0242. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-242 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0243. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-243 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0244. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-244 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0245. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-245 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0246. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-246 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0247. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-247 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0248. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-248 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0249. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-249 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0250. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-250 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0251. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-251 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0252. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-252 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0253. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-253 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0254. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-254 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0255. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-255 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0256. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-256 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0257. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-257 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0258. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-258 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0259. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-259 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0260. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-260 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0261. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-261 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0262. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-262 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0263. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-263 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0264. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-264 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0265. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-265 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0266. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-266 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0267. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-267 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0268. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-268 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0269. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-269 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0270. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-270 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0271. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-271 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0272. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-272 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0273. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-273 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0274. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-274 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0275. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-275 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0276. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-276 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0277. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-277 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0278. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-278 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0279. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-279 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0280. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-280 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0281. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-281 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0282. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-282 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0283. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-283 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0284. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-284 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0285. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-285 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0286. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-286 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0287. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-287 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0288. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-288 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0289. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-289 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0290. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-290 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0291. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-291 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0292. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-292 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0293. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-293 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0294. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-294 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0295. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-295 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0296. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-296 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0297. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-297 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0298. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-298 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0299. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-299 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0300. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-300 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0301. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-301 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0302. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-302 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0303. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-303 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0304. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-304 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0305. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-305 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0306. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-306 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0307. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-307 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0308. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-308 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0309. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-309 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0310. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-310 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0311. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-311 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0312. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-312 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0313. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-313 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0314. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-314 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0315. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-315 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0316. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-316 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0317. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-317 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0318. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-318 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0319. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-319 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0320. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-320 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0321. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-321 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0322. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-322 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0323. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-323 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0324. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-324 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0325. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-325 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0326. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-326 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0327. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-327 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0328. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-328 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0329. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-329 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0330. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-330 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0331. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-331 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0332. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-332 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0333. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-333 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0334. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-334 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0335. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-335 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0336. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-336 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0337. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-337 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0338. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-338 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0339. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-339 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0340. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-340 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0341. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-341 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0342. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-342 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0343. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-343 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0344. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-344 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0345. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-345 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0346. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-346 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0347. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-347 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0348. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-348 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0349. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-349 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0350. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-350 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0351. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-351 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0352. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-352 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0353. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-353 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0354. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-354 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0355. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-355 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0356. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-356 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0357. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-357 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0358. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-358 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0359. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-359 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0360. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-360 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0361. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-361 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0362. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-362 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0363. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-363 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0364. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-364 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0365. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-365 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0366. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-366 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0367. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-367 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0368. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-368 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0369. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-369 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0370. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-370 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0371. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-371 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0372. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-372 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0373. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-373 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0374. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-374 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0375. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-375 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0376. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-376 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0377. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-377 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0378. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-378 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0379. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-379 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0380. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-380 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0381. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-381 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0382. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-382 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0383. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-383 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0384. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-384 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0385. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-385 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0386. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-386 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0387. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-387 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0388. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-388 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0389. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-389 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0390. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-390 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0391. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-391 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0392. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-392 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0393. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-393 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0394. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-394 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0395. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-395 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0396. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-396 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0397. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-397 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0398. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-398 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0399. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-399 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0400. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-400 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0401. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-401 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0402. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-402 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0403. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-403 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0404. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-404 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0405. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-405 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0406. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-406 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0407. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-407 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0408. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-408 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0409. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-409 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0410. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-410 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0411. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-411 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0412. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-412 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0413. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-413 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0414. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-414 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0415. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-415 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0416. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-416 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0417. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-417 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0418. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-418 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0419. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-419 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0420. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-420 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0421. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-421 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0422. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-422 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0423. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-423 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0424. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-424 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0425. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-425 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0426. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-426 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0427. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-427 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0428. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-428 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0429. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-429 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0430. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-430 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0431. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-431 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0432. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-432 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0433. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-433 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0434. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-434 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0435. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-435 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0436. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-436 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0437. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-437 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0438. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-438 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0439. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-439 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0440. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-440 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0441. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-441 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0442. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-442 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0443. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-443 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0444. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-444 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0445. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-445 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0446. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-446 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0447. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-447 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0448. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-448 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0449. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-449 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0450. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-450 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0451. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-451 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0452. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-452 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0453. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-453 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0454. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-454 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0455. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-455 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0456. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-456 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0457. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-457 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0458. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-458 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0459. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-459 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0460. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-460 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0461. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-461 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0462. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-462 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0463. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-463 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0464. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-464 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0465. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-465 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0466. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-466 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0467. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-467 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0468. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-468 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0469. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-469 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0470. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-470 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0471. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-471 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0472. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-472 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0473. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-473 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0474. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-474 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0475. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-475 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0476. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-476 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0477. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-477 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0478. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-478 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0479. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-479 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0480. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-480 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0481. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-481 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0482. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-482 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0483. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-483 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0484. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-484 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0485. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-485 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0486. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-486 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0487. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-487 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0488. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-488 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0489. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-489 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0490. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-490 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0491. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-491 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0492. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-492 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0493. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-493 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0494. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-494 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0495. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-495 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0496. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-496 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0497. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-497 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0498. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-498 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0499. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-499 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0500. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-500 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0501. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-501 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0502. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-502 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0503. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-503 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0504. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-504 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0505. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-505 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0506. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-506 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0507. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-507 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0508. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-508 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0509. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-509 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0510. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-510 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0511. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-511 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0512. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-512 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0513. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-513 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0514. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-514 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0515. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-515 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0516. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-516 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0517. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-517 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0518. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-518 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0519. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-519 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0520. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-520 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0521. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-521 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0522. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-522 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0523. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-523 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0524. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-524 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0525. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-525 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0526. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-526 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0527. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-527 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0528. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-528 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0529. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-529 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0530. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-530 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0531. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-531 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0532. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-532 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0533. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-533 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0534. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-534 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0535. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-535 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0536. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-536 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0537. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-537 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0538. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-538 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0539. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-539 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0540. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-540 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0541. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-541 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0542. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-542 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0543. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-543 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0544. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-544 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0545. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-545 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0546. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-546 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0547. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-547 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0548. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-548 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0549. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-549 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0550. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-550 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0551. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-551 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0552. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-552 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0553. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-553 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0554. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-554 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0555. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-555 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0556. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-556 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0557. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-557 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0558. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-558 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0559. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-559 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0560. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-560 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0561. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-561 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0562. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-562 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0563. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-563 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0564. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-564 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0565. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-565 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0566. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-566 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0567. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-567 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0568. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-568 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0569. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-569 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0570. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-570 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0571. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-571 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0572. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-572 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0573. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-573 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0574. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-574 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0575. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-575 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0576. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-576 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0577. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-577 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0578. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-578 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0579. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-579 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0580. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-580 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0581. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-581 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0582. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-582 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0583. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-583 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0584. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-584 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0585. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-585 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0586. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-586 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0587. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-587 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0588. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-588 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0589. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-589 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0590. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-590 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0591. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-591 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0592. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-592 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0593. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-593 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0594. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-594 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0595. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-595 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0596. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-596 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0597. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-597 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0598. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-598 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0599. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-599 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0600. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-600 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0601. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-601 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0602. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-602 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0603. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-603 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0604. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-604 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0605. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-605 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0606. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-606 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0607. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-607 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0608. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-608 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0609. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-609 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0610. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-610 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0611. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-611 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0612. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-612 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0613. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-613 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0614. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-614 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0615. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-615 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0616. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-616 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0617. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-617 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0618. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-618 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0619. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-619 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0620. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-620 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0621. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-621 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0622. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-622 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0623. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-623 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0624. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-624 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0625. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-625 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0626. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-626 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0627. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-627 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0628. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-628 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0629. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-629 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0630. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-630 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0631. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-631 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0632. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-632 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0633. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-633 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0634. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-634 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0635. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-635 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0636. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-636 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0637. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-637 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0638. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-638 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0639. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-639 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0640. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-640 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0641. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-641 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0642. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-642 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0643. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-643 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0644. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-644 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0645. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-645 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0646. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-646 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0647. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-647 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0648. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-648 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0649. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-649 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0650. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-650 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0651. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-651 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0652. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-652 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0653. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-653 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0654. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-654 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0655. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-655 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0656. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-656 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0657. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-657 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0658. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-658 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0659. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-659 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0660. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-660 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0661. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-661 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0662. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-662 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0663. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-663 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0664. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-664 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0665. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-665 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0666. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-666 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0667. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-667 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0668. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-668 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0669. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-669 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0670. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-670 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0671. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-671 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0672. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-672 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0673. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-673 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0674. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-674 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0675. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-675 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0676. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-676 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0677. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-677 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0678. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-678 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0679. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-679 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0680. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-680 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0681. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-681 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0682. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-682 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0683. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-683 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0684. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-684 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0685. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-685 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0686. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-686 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0687. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-687 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0688. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-688 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0689. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-689 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0690. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-690 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0691. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-691 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0692. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-692 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0693. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-693 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0694. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-694 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0695. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-695 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0696. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-696 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0697. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-697 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0698. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-698 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0699. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-699 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0700. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-700 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0701. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-701 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0702. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-702 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0703. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-703 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0704. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-704 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0705. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-705 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0706. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-706 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0707. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-707 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0708. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-708 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0709. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-709 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0710. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-710 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0711. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-711 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0712. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-712 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0713. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-713 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0714. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-714 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0715. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-715 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0716. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-716 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0717. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-717 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0718. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-718 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0719. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-719 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0720. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-720 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0721. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-721 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0722. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-722 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0723. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-723 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0724. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-724 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0725. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-725 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0726. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-726 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0727. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-727 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0728. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-728 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0729. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-729 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0730. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-730 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0731. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-731 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0732. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-732 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0733. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-733 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0734. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-734 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0735. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-735 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0736. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-736 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0737. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-737 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0738. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-738 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0739. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-739 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0740. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-740 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0741. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-741 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0742. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-742 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0743. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-743 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0744. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-744 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0745. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-745 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0746. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-746 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0747. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-747 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0748. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-748 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0749. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-749 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0750. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-750 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0751. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-751 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0752. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-752 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0753. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-753 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0754. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-754 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0755. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-755 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0756. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-756 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0757. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-757 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0758. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-758 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0759. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-759 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0760. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-760 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0761. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-761 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0762. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-762 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0763. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-763 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0764. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-764 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0765. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-765 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0766. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-766 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0767. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-767 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0768. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-768 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0769. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-769 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0770. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-770 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0771. Systemd — Inspect
**Objective:** inspect the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-771 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0772. Storage — Document
**Objective:** document the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-772 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0773. Thermal Limits — Test
**Objective:** test the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-773 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0774. Arm64 — Compare
**Objective:** compare the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-774 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0775. Gpio — Isolate
**Objective:** isolate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-775 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0776. Networking — Record
**Objective:** record the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-776 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0777. Systemd — Validate
**Objective:** validate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-777 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0778. Storage — Review
**Objective:** review the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-778 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0779. Thermal Limits — Restore
**Objective:** restore the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-779 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0780. Arm64 — Verify
**Objective:** verify the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-780 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0781. Gpio — Inspect
**Objective:** inspect the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-781 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0782. Networking — Document
**Objective:** document the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-782 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0783. Systemd — Test
**Objective:** test the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-783 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0784. Storage — Compare
**Objective:** compare the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-784 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0785. Thermal Limits — Isolate
**Objective:** isolate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-785 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0786. Arm64 — Record
**Objective:** record the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-786 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0787. Gpio — Validate
**Objective:** validate the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-787 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0788. Networking — Review
**Objective:** review the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-788 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0789. Systemd — Restore
**Objective:** restore the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-789 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0790. Storage — Verify
**Objective:** verify the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-790 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0791. Thermal Limits — Inspect
**Objective:** inspect the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-791 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0792. Arm64 — Document
**Objective:** document the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-792 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0793. Gpio — Test
**Objective:** test the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-793 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0794. Networking — Compare
**Objective:** compare the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-794 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

## 0795. Systemd — Isolate
**Objective:** isolate the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-795 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include storage and thermal limits.

## 0796. Storage — Record
**Objective:** record the storage behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of storage. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If storage behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-796 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include thermal limits and ARM64.

## 0797. Thermal Limits — Validate
**Objective:** validate the thermal limits behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of thermal limits. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If thermal limits behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-797 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include ARM64 and GPIO.

## 0798. Arm64 — Review
**Objective:** review the ARM64 behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of ARM64. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If ARM64 behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-798 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include GPIO and networking.

## 0799. Gpio — Restore
**Objective:** restore the GPIO behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of GPIO. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If GPIO behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-799 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include networking and systemd.

## 0800. Networking — Verify
**Objective:** verify the networking behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of networking. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If networking behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 9-800 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and storage.

