# Angel Knowledge Field Guide: Security Hardening

This field guide is operational reference material for Angel. It emphasizes observable evidence, reversible changes, least privilege, and explicit user approval before impactful actions.

## 0001. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-1 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0002. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-2 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0003. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-3 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0004. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-4 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0005. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-5 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0006. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-6 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0007. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-7 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0008. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-8 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0009. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-9 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0010. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-10 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0011. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-11 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0012. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-12 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0013. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-13 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0014. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-14 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0015. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-15 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0016. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-16 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0017. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-17 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0018. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-18 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0019. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-19 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0020. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-20 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0021. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-21 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0022. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-22 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0023. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-23 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0024. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-24 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0025. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-25 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0026. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-26 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0027. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-27 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0028. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-28 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0029. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-29 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0030. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-30 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0031. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-31 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0032. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-32 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0033. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-33 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0034. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-34 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0035. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-35 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0036. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-36 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0037. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-37 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0038. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-38 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0039. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-39 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0040. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-40 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0041. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-41 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0042. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-42 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0043. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-43 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0044. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-44 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0045. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-45 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0046. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-46 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0047. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-47 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0048. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-48 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0049. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-49 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0050. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-50 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0051. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-51 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0052. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-52 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0053. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-53 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0054. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-54 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0055. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-55 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0056. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-56 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0057. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-57 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0058. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-58 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0059. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-59 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0060. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-60 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0061. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-61 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0062. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-62 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0063. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-63 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0064. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-64 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0065. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-65 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0066. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-66 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0067. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-67 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0068. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-68 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0069. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-69 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0070. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-70 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0071. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-71 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0072. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-72 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0073. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-73 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0074. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-74 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0075. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-75 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0076. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-76 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0077. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-77 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0078. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-78 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0079. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-79 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0080. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-80 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0081. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-81 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0082. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-82 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0083. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-83 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0084. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-84 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0085. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-85 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0086. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-86 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0087. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-87 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0088. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-88 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0089. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-89 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0090. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-90 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0091. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-91 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0092. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-92 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0093. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-93 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0094. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-94 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0095. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-95 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0096. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-96 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0097. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-97 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0098. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-98 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0099. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-99 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0100. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-100 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0101. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-101 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0102. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-102 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0103. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-103 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0104. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-104 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0105. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-105 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0106. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-106 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0107. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-107 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0108. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-108 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0109. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-109 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0110. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-110 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0111. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-111 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0112. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-112 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0113. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-113 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0114. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-114 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0115. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-115 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0116. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-116 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0117. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-117 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0118. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-118 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0119. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-119 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0120. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-120 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0121. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-121 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0122. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-122 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0123. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-123 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0124. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-124 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0125. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-125 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0126. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-126 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0127. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-127 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0128. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-128 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0129. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-129 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0130. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-130 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0131. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-131 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0132. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-132 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0133. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-133 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0134. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-134 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0135. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-135 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0136. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-136 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0137. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-137 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0138. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-138 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0139. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-139 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0140. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-140 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0141. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-141 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0142. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-142 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0143. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-143 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0144. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-144 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0145. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-145 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0146. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-146 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0147. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-147 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0148. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-148 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0149. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-149 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0150. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-150 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0151. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-151 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0152. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-152 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0153. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-153 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0154. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-154 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0155. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-155 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0156. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-156 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0157. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-157 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0158. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-158 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0159. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-159 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0160. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-160 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0161. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-161 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0162. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-162 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0163. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-163 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0164. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-164 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0165. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-165 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0166. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-166 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0167. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-167 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0168. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-168 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0169. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-169 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0170. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-170 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0171. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-171 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0172. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-172 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0173. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-173 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0174. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-174 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0175. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-175 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0176. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-176 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0177. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-177 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0178. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-178 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0179. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-179 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0180. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-180 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0181. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-181 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0182. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-182 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0183. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-183 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0184. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-184 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0185. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-185 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0186. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-186 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0187. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-187 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0188. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-188 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0189. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-189 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0190. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-190 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0191. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-191 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0192. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-192 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0193. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-193 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0194. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-194 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0195. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-195 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0196. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-196 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0197. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-197 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0198. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-198 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0199. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-199 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0200. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-200 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0201. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-201 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0202. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-202 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0203. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-203 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0204. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-204 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0205. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-205 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0206. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-206 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0207. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-207 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0208. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-208 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0209. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-209 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0210. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-210 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0211. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-211 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0212. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-212 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0213. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-213 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0214. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-214 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0215. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-215 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0216. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-216 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0217. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-217 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0218. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-218 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0219. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-219 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0220. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-220 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0221. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-221 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0222. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-222 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0223. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-223 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0224. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-224 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0225. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-225 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0226. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-226 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0227. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-227 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0228. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-228 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0229. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-229 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0230. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-230 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0231. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-231 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0232. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-232 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0233. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-233 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0234. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-234 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0235. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-235 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0236. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-236 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0237. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-237 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0238. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-238 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0239. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-239 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0240. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-240 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0241. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-241 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0242. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-242 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0243. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-243 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0244. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-244 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0245. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-245 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0246. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-246 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0247. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-247 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0248. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-248 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0249. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-249 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0250. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-250 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0251. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-251 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0252. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-252 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0253. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-253 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0254. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-254 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0255. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-255 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0256. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-256 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0257. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-257 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0258. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-258 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0259. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-259 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0260. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-260 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0261. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-261 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0262. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-262 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0263. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-263 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0264. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-264 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0265. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-265 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0266. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-266 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0267. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-267 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0268. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-268 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0269. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-269 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0270. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-270 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0271. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-271 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0272. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-272 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0273. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-273 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0274. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-274 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0275. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-275 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0276. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-276 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0277. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-277 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0278. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-278 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0279. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-279 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0280. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-280 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0281. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-281 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0282. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-282 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0283. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-283 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0284. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-284 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0285. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-285 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0286. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-286 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0287. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-287 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0288. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-288 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0289. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-289 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0290. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-290 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0291. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-291 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0292. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-292 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0293. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-293 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0294. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-294 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0295. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-295 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0296. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-296 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0297. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-297 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0298. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-298 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0299. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-299 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0300. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-300 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0301. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-301 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0302. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-302 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0303. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-303 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0304. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-304 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0305. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-305 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0306. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-306 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0307. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-307 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0308. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-308 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0309. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-309 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0310. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-310 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0311. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-311 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0312. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-312 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0313. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-313 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0314. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-314 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0315. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-315 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0316. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-316 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0317. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-317 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0318. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-318 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0319. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-319 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0320. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-320 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0321. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-321 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0322. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-322 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0323. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-323 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0324. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-324 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0325. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-325 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0326. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-326 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0327. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-327 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0328. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-328 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0329. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-329 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0330. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-330 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0331. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-331 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0332. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-332 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0333. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-333 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0334. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-334 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0335. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-335 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0336. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-336 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0337. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-337 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0338. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-338 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0339. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-339 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0340. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-340 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0341. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-341 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0342. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-342 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0343. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-343 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0344. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-344 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0345. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-345 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0346. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-346 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0347. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-347 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0348. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-348 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0349. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-349 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0350. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-350 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0351. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-351 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0352. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-352 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0353. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-353 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0354. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-354 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0355. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-355 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0356. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-356 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0357. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-357 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0358. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-358 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0359. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-359 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0360. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-360 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0361. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-361 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0362. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-362 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0363. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-363 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0364. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-364 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0365. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-365 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0366. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-366 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0367. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-367 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0368. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-368 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0369. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-369 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0370. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-370 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0371. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-371 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0372. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-372 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0373. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-373 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0374. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-374 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0375. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-375 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0376. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-376 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0377. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-377 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0378. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-378 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0379. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-379 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0380. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-380 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0381. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-381 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0382. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-382 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0383. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-383 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0384. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-384 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0385. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-385 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0386. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-386 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0387. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-387 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0388. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-388 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0389. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-389 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0390. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-390 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0391. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-391 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0392. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-392 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0393. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-393 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0394. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-394 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0395. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-395 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0396. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-396 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0397. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-397 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0398. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-398 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0399. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-399 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0400. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-400 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0401. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-401 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0402. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-402 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0403. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-403 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0404. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-404 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0405. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-405 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0406. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-406 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0407. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-407 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0408. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-408 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0409. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-409 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0410. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-410 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0411. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-411 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0412. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-412 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0413. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-413 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0414. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-414 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0415. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-415 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0416. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-416 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0417. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-417 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0418. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-418 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0419. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-419 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0420. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-420 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0421. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-421 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0422. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-422 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0423. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-423 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0424. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-424 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0425. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-425 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0426. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-426 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0427. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-427 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0428. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-428 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0429. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-429 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0430. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-430 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0431. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-431 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0432. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-432 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0433. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-433 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0434. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-434 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0435. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-435 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0436. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-436 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0437. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-437 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0438. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-438 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0439. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-439 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0440. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-440 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0441. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-441 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0442. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-442 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0443. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-443 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0444. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-444 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0445. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-445 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0446. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-446 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0447. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-447 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0448. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-448 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0449. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-449 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0450. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-450 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0451. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-451 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0452. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-452 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0453. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-453 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0454. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-454 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0455. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-455 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0456. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-456 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0457. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-457 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0458. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-458 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0459. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-459 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0460. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-460 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0461. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-461 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0462. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-462 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0463. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-463 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0464. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-464 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0465. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-465 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0466. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-466 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0467. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-467 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0468. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-468 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0469. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-469 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0470. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-470 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0471. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-471 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0472. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-472 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0473. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-473 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0474. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-474 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0475. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-475 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0476. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-476 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0477. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-477 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0478. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-478 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0479. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-479 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0480. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-480 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0481. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-481 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0482. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-482 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0483. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-483 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0484. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-484 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0485. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-485 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0486. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-486 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0487. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-487 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0488. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-488 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0489. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-489 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0490. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-490 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0491. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-491 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0492. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-492 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0493. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-493 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0494. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-494 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0495. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-495 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0496. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-496 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0497. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-497 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0498. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-498 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0499. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-499 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0500. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-500 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0501. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-501 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0502. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-502 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0503. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-503 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0504. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-504 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0505. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-505 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0506. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-506 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0507. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-507 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0508. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-508 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0509. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-509 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0510. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-510 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0511. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-511 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0512. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-512 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0513. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-513 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0514. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-514 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0515. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-515 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0516. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-516 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0517. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-517 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0518. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-518 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0519. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-519 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0520. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-520 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0521. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-521 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0522. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-522 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0523. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-523 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0524. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-524 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0525. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-525 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0526. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-526 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0527. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-527 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0528. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-528 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0529. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-529 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0530. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-530 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0531. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-531 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0532. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-532 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0533. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-533 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0534. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-534 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0535. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-535 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0536. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-536 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0537. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-537 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0538. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-538 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0539. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-539 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0540. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-540 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0541. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-541 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0542. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-542 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0543. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-543 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0544. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-544 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0545. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-545 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0546. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-546 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0547. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-547 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0548. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-548 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0549. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-549 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0550. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-550 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0551. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-551 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0552. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-552 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0553. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-553 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0554. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-554 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0555. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-555 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0556. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-556 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0557. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-557 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0558. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-558 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0559. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-559 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0560. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-560 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0561. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-561 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0562. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-562 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0563. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-563 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0564. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-564 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0565. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-565 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0566. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-566 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0567. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-567 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0568. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-568 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0569. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-569 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0570. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-570 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0571. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-571 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0572. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-572 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0573. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-573 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0574. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-574 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0575. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-575 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0576. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-576 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0577. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-577 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0578. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-578 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0579. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-579 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0580. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-580 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0581. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-581 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0582. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-582 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0583. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-583 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0584. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-584 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0585. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-585 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0586. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-586 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0587. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-587 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0588. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-588 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0589. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-589 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0590. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-590 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0591. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-591 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0592. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-592 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0593. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-593 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0594. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-594 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0595. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-595 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0596. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-596 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0597. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-597 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0598. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-598 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0599. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-599 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0600. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-600 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0601. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-601 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0602. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-602 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0603. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-603 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0604. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-604 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0605. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-605 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0606. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-606 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0607. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-607 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0608. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-608 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0609. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-609 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0610. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-610 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0611. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-611 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0612. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-612 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0613. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-613 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0614. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-614 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0615. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-615 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0616. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-616 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0617. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-617 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0618. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-618 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0619. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-619 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0620. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-620 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0621. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-621 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0622. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-622 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0623. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-623 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0624. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-624 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0625. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-625 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0626. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-626 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0627. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-627 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0628. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-628 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0629. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-629 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0630. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-630 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0631. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-631 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0632. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-632 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0633. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-633 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0634. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-634 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0635. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-635 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0636. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-636 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0637. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-637 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0638. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-638 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0639. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-639 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0640. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-640 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0641. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-641 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0642. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-642 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0643. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-643 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0644. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-644 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0645. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-645 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0646. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-646 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0647. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-647 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0648. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-648 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0649. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-649 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0650. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-650 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0651. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-651 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0652. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-652 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0653. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-653 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0654. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-654 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0655. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-655 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0656. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-656 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0657. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-657 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0658. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-658 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0659. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-659 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0660. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-660 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0661. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-661 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0662. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-662 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0663. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-663 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0664. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-664 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0665. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-665 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0666. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-666 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0667. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-667 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0668. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-668 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0669. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-669 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0670. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-670 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0671. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-671 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0672. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-672 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0673. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-673 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0674. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-674 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0675. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-675 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0676. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-676 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0677. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-677 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0678. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-678 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0679. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-679 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0680. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-680 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0681. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-681 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0682. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-682 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0683. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-683 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0684. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-684 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0685. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-685 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0686. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-686 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0687. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-687 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0688. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-688 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0689. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-689 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0690. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-690 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0691. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-691 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0692. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-692 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0693. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-693 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0694. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-694 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0695. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-695 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0696. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-696 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0697. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-697 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0698. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-698 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0699. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-699 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0700. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-700 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0701. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-701 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0702. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-702 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0703. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-703 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0704. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-704 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0705. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-705 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0706. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-706 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0707. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-707 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0708. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-708 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0709. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-709 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0710. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-710 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0711. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-711 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0712. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-712 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0713. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-713 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0714. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-714 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0715. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-715 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0716. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-716 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0717. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-717 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0718. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-718 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0719. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-719 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0720. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-720 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0721. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-721 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0722. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-722 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0723. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-723 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0724. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-724 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0725. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-725 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0726. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-726 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0727. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-727 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0728. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-728 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0729. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-729 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0730. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-730 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0731. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-731 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0732. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-732 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0733. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-733 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0734. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-734 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0735. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-735 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0736. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-736 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0737. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-737 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0738. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-738 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0739. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-739 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0740. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-740 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0741. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-741 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0742. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-742 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0743. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-743 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0744. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-744 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0745. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-745 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0746. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-746 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0747. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-747 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0748. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-748 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0749. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-749 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0750. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-750 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0751. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-751 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0752. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-752 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0753. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-753 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0754. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-754 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0755. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-755 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0756. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-756 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0757. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-757 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0758. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-758 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0759. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-759 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0760. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-760 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0761. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-761 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0762. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-762 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0763. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-763 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0764. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-764 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0765. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-765 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0766. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-766 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0767. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-767 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0768. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-768 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0769. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-769 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0770. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-770 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0771. Sandboxing — Inspect
**Objective:** inspect the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-771 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0772. Updates — Document
**Objective:** document the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-772 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0773. Backups — Test
**Objective:** test the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-773 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0774. Least Privilege — Compare
**Objective:** compare the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-774 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0775. Input Validation — Isolate
**Objective:** isolate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-775 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0776. Secrets — Record
**Objective:** record the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-776 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0777. Sandboxing — Validate
**Objective:** validate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-777 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0778. Updates — Review
**Objective:** review the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-778 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0779. Backups — Restore
**Objective:** restore the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-779 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0780. Least Privilege — Verify
**Objective:** verify the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-780 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0781. Input Validation — Inspect
**Objective:** inspect the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-781 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0782. Secrets — Document
**Objective:** document the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-782 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0783. Sandboxing — Test
**Objective:** test the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-783 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0784. Updates — Compare
**Objective:** compare the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-784 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0785. Backups — Isolate
**Objective:** isolate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-785 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0786. Least Privilege — Record
**Objective:** record the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-786 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0787. Input Validation — Validate
**Objective:** validate the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-787 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0788. Secrets — Review
**Objective:** review the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-788 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0789. Sandboxing — Restore
**Objective:** restore the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-789 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0790. Updates — Verify
**Objective:** verify the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-790 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0791. Backups — Inspect
**Objective:** inspect the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-791 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0792. Least Privilege — Document
**Objective:** document the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-792 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0793. Input Validation — Test
**Objective:** test the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-793 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0794. Secrets — Compare
**Objective:** compare the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-794 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

## 0795. Sandboxing — Isolate
**Objective:** isolate the sandboxing behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of sandboxing. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If sandboxing behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-795 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include updates and backups.

## 0796. Updates — Record
**Objective:** record the updates behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of updates. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If updates behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-796 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include backups and least privilege.

## 0797. Backups — Validate
**Objective:** validate the backups behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of backups. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If backups behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-797 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include least privilege and input validation.

## 0798. Least Privilege — Review
**Objective:** review the least privilege behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of least privilege. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If least privilege behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-798 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include input validation and secrets.

## 0799. Input Validation — Restore
**Objective:** restore the input validation behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of input validation. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If input validation behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-799 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include secrets and sandboxing.

## 0800. Secrets — Verify
**Objective:** verify the secrets behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of secrets. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If secrets behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 11-800 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include sandboxing and updates.

