# Angel Knowledge Field Guide: Linux Admin

This field guide is operational reference material for Angel. It emphasizes observable evidence, reversible changes, least privilege, and explicit user approval before impactful actions.

## 0001. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-1 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0002. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-2 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0003. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-3 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0004. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-4 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0005. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-5 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0006. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-6 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0007. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-7 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0008. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-8 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0009. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-9 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0010. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-10 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0011. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-11 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0012. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-12 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0013. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-13 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0014. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-14 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0015. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-15 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0016. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-16 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0017. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-17 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0018. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-18 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0019. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-19 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0020. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-20 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0021. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-21 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0022. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-22 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0023. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-23 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0024. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-24 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0025. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-25 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0026. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-26 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0027. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-27 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0028. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-28 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0029. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-29 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0030. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-30 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0031. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-31 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0032. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-32 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0033. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-33 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0034. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-34 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0035. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-35 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0036. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-36 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0037. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-37 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0038. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-38 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0039. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-39 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0040. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-40 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0041. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-41 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0042. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-42 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0043. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-43 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0044. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-44 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0045. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-45 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0046. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-46 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0047. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-47 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0048. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-48 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0049. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-49 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0050. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-50 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0051. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-51 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0052. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-52 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0053. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-53 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0054. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-54 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0055. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-55 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0056. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-56 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0057. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-57 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0058. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-58 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0059. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-59 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0060. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-60 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0061. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-61 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0062. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-62 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0063. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-63 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0064. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-64 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0065. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-65 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0066. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-66 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0067. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-67 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0068. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-68 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0069. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-69 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0070. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-70 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0071. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-71 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0072. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-72 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0073. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-73 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0074. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-74 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0075. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-75 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0076. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-76 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0077. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-77 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0078. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-78 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0079. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-79 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0080. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-80 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0081. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-81 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0082. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-82 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0083. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-83 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0084. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-84 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0085. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-85 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0086. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-86 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0087. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-87 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0088. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-88 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0089. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-89 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0090. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-90 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0091. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-91 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0092. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-92 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0093. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-93 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0094. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-94 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0095. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-95 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0096. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-96 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0097. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-97 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0098. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-98 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0099. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-99 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0100. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-100 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0101. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-101 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0102. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-102 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0103. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-103 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0104. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-104 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0105. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-105 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0106. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-106 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0107. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-107 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0108. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-108 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0109. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-109 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0110. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-110 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0111. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-111 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0112. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-112 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0113. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-113 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0114. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-114 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0115. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-115 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0116. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-116 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0117. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-117 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0118. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-118 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0119. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-119 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0120. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-120 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0121. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-121 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0122. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-122 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0123. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-123 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0124. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-124 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0125. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-125 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0126. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-126 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0127. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-127 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0128. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-128 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0129. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-129 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0130. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-130 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0131. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-131 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0132. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-132 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0133. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-133 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0134. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-134 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0135. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-135 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0136. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-136 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0137. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-137 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0138. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-138 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0139. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-139 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0140. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-140 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0141. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-141 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0142. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-142 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0143. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-143 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0144. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-144 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0145. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-145 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0146. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-146 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0147. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-147 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0148. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-148 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0149. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-149 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0150. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-150 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0151. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-151 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0152. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-152 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0153. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-153 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0154. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-154 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0155. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-155 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0156. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-156 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0157. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-157 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0158. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-158 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0159. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-159 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0160. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-160 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0161. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-161 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0162. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-162 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0163. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-163 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0164. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-164 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0165. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-165 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0166. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-166 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0167. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-167 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0168. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-168 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0169. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-169 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0170. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-170 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0171. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-171 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0172. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-172 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0173. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-173 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0174. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-174 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0175. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-175 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0176. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-176 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0177. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-177 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0178. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-178 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0179. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-179 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0180. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-180 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0181. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-181 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0182. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-182 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0183. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-183 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0184. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-184 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0185. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-185 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0186. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-186 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0187. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-187 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0188. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-188 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0189. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-189 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0190. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-190 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0191. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-191 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0192. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-192 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0193. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-193 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0194. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-194 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0195. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-195 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0196. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-196 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0197. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-197 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0198. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-198 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0199. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-199 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0200. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-200 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0201. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-201 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0202. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-202 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0203. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-203 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0204. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-204 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0205. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-205 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0206. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-206 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0207. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-207 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0208. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-208 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0209. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-209 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0210. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-210 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0211. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-211 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0212. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-212 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0213. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-213 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0214. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-214 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0215. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-215 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0216. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-216 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0217. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-217 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0218. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-218 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0219. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-219 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0220. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-220 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0221. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-221 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0222. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-222 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0223. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-223 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0224. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-224 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0225. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-225 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0226. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-226 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0227. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-227 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0228. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-228 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0229. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-229 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0230. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-230 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0231. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-231 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0232. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-232 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0233. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-233 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0234. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-234 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0235. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-235 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0236. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-236 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0237. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-237 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0238. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-238 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0239. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-239 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0240. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-240 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0241. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-241 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0242. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-242 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0243. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-243 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0244. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-244 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0245. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-245 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0246. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-246 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0247. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-247 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0248. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-248 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0249. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-249 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0250. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-250 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0251. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-251 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0252. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-252 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0253. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-253 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0254. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-254 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0255. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-255 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0256. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-256 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0257. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-257 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0258. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-258 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0259. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-259 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0260. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-260 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0261. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-261 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0262. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-262 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0263. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-263 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0264. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-264 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0265. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-265 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0266. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-266 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0267. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-267 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0268. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-268 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0269. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-269 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0270. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-270 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0271. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-271 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0272. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-272 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0273. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-273 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0274. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-274 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0275. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-275 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0276. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-276 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0277. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-277 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0278. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-278 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0279. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-279 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0280. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-280 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0281. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-281 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0282. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-282 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0283. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-283 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0284. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-284 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0285. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-285 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0286. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-286 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0287. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-287 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0288. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-288 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0289. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-289 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0290. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-290 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0291. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-291 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0292. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-292 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0293. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-293 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0294. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-294 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0295. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-295 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0296. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-296 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0297. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-297 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0298. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-298 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0299. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-299 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0300. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-300 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0301. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-301 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0302. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-302 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0303. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-303 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0304. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-304 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0305. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-305 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0306. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-306 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0307. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-307 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0308. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-308 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0309. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-309 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0310. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-310 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0311. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-311 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0312. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-312 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0313. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-313 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0314. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-314 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0315. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-315 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0316. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-316 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0317. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-317 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0318. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-318 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0319. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-319 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0320. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-320 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0321. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-321 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0322. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-322 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0323. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-323 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0324. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-324 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0325. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-325 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0326. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-326 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0327. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-327 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0328. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-328 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0329. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-329 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0330. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-330 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0331. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-331 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0332. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-332 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0333. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-333 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0334. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-334 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0335. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-335 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0336. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-336 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0337. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-337 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0338. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-338 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0339. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-339 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0340. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-340 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0341. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-341 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0342. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-342 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0343. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-343 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0344. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-344 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0345. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-345 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0346. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-346 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0347. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-347 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0348. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-348 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0349. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-349 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0350. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-350 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0351. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-351 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0352. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-352 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0353. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-353 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0354. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-354 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0355. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-355 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0356. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-356 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0357. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-357 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0358. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-358 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0359. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-359 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0360. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-360 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0361. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-361 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0362. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-362 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0363. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-363 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0364. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-364 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0365. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-365 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0366. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-366 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0367. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-367 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0368. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-368 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0369. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-369 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0370. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-370 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0371. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-371 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0372. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-372 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0373. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-373 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0374. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-374 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0375. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-375 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0376. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-376 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0377. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-377 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0378. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-378 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0379. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-379 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0380. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-380 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0381. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-381 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0382. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-382 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0383. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-383 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0384. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-384 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0385. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-385 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0386. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-386 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0387. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-387 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0388. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-388 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0389. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-389 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0390. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-390 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0391. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-391 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0392. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-392 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0393. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-393 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0394. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-394 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0395. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-395 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0396. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-396 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0397. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-397 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0398. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-398 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0399. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-399 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0400. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-400 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0401. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-401 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0402. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-402 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0403. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-403 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0404. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-404 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0405. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-405 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0406. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-406 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0407. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-407 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0408. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-408 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0409. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-409 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0410. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-410 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0411. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-411 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0412. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-412 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0413. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-413 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0414. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-414 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0415. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-415 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0416. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-416 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0417. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-417 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0418. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-418 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0419. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-419 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0420. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-420 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0421. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-421 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0422. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-422 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0423. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-423 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0424. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-424 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0425. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-425 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0426. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-426 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0427. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-427 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0428. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-428 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0429. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-429 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0430. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-430 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0431. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-431 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0432. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-432 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0433. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-433 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0434. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-434 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0435. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-435 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0436. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-436 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0437. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-437 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0438. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-438 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0439. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-439 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0440. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-440 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0441. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-441 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0442. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-442 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0443. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-443 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0444. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-444 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0445. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-445 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0446. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-446 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0447. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-447 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0448. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-448 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0449. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-449 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0450. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-450 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0451. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-451 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0452. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-452 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0453. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-453 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0454. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-454 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0455. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-455 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0456. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-456 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0457. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-457 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0458. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-458 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0459. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-459 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0460. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-460 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0461. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-461 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0462. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-462 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0463. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-463 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0464. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-464 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0465. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-465 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0466. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-466 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0467. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-467 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0468. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-468 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0469. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-469 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0470. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-470 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0471. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-471 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0472. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-472 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0473. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-473 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0474. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-474 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0475. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-475 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0476. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-476 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0477. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-477 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0478. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-478 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0479. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-479 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0480. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-480 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0481. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-481 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0482. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-482 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0483. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-483 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0484. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-484 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0485. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-485 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0486. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-486 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0487. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-487 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0488. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-488 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0489. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-489 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0490. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-490 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0491. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-491 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0492. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-492 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0493. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-493 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0494. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-494 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0495. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-495 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0496. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-496 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0497. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-497 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0498. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-498 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0499. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-499 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0500. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-500 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0501. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-501 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0502. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-502 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0503. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-503 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0504. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-504 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0505. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-505 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0506. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-506 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0507. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-507 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0508. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-508 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0509. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-509 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0510. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-510 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0511. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-511 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0512. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-512 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0513. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-513 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0514. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-514 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0515. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-515 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0516. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-516 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0517. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-517 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0518. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-518 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0519. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-519 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0520. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-520 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0521. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-521 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0522. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-522 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0523. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-523 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0524. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-524 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0525. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-525 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0526. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-526 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0527. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-527 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0528. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-528 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0529. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-529 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0530. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-530 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0531. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-531 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0532. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-532 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0533. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-533 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0534. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-534 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0535. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-535 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0536. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-536 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0537. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-537 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0538. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-538 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0539. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-539 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0540. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-540 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0541. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-541 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0542. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-542 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0543. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-543 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0544. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-544 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0545. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-545 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0546. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-546 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0547. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-547 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0548. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-548 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0549. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-549 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0550. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-550 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0551. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-551 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0552. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-552 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0553. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-553 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0554. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-554 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0555. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-555 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0556. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-556 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0557. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-557 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0558. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-558 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0559. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-559 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0560. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-560 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0561. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-561 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0562. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-562 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0563. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-563 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0564. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-564 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0565. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-565 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0566. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-566 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0567. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-567 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0568. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-568 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0569. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-569 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0570. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-570 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0571. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-571 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0572. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-572 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0573. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-573 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0574. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-574 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0575. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-575 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0576. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-576 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0577. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-577 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0578. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-578 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0579. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-579 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0580. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-580 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0581. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-581 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0582. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-582 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0583. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-583 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0584. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-584 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0585. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-585 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0586. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-586 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0587. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-587 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0588. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-588 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0589. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-589 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0590. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-590 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0591. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-591 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0592. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-592 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0593. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-593 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0594. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-594 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0595. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-595 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0596. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-596 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0597. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-597 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0598. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-598 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0599. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-599 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0600. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-600 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0601. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-601 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0602. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-602 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0603. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-603 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0604. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-604 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0605. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-605 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0606. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-606 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0607. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-607 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0608. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-608 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0609. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-609 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0610. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-610 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0611. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-611 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0612. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-612 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0613. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-613 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0614. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-614 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0615. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-615 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0616. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-616 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0617. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-617 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0618. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-618 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0619. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-619 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0620. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-620 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0621. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-621 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0622. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-622 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0623. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-623 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0624. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-624 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0625. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-625 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0626. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-626 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0627. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-627 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0628. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-628 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0629. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-629 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0630. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-630 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0631. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-631 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0632. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-632 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0633. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-633 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0634. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-634 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0635. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-635 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0636. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-636 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0637. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-637 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0638. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-638 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0639. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-639 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0640. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-640 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0641. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-641 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0642. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-642 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0643. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-643 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0644. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-644 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0645. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-645 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0646. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-646 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0647. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-647 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0648. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-648 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0649. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-649 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0650. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-650 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0651. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-651 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0652. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-652 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0653. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-653 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0654. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-654 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0655. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-655 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0656. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-656 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0657. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-657 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0658. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-658 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0659. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-659 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0660. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-660 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0661. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-661 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0662. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-662 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0663. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-663 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0664. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-664 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0665. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-665 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0666. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-666 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0667. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-667 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0668. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-668 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0669. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-669 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0670. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-670 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0671. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-671 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0672. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-672 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0673. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-673 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0674. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-674 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0675. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-675 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0676. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-676 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0677. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-677 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0678. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-678 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0679. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-679 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0680. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-680 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0681. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-681 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0682. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-682 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0683. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-683 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0684. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-684 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0685. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-685 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0686. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-686 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0687. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-687 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0688. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-688 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0689. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-689 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0690. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-690 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0691. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-691 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0692. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-692 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0693. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-693 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0694. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-694 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0695. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-695 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0696. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-696 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0697. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-697 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0698. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-698 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0699. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-699 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0700. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-700 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0701. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-701 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0702. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-702 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0703. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-703 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0704. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-704 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0705. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-705 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0706. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-706 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0707. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-707 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0708. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-708 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0709. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-709 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0710. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-710 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0711. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-711 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0712. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-712 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0713. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-713 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0714. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-714 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0715. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-715 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0716. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-716 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0717. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-717 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0718. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-718 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0719. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-719 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0720. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-720 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0721. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-721 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0722. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-722 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0723. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-723 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0724. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-724 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0725. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-725 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0726. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-726 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0727. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-727 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0728. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-728 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0729. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-729 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0730. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-730 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0731. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-731 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0732. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-732 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0733. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-733 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0734. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-734 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0735. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-735 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0736. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-736 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0737. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-737 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0738. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-738 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0739. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-739 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0740. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-740 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0741. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-741 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0742. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-742 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0743. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-743 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0744. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-744 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0745. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-745 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0746. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-746 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0747. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-747 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0748. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-748 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0749. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-749 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0750. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-750 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0751. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-751 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0752. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-752 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0753. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-753 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0754. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-754 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0755. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-755 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0756. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-756 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0757. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-757 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0758. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-758 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0759. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-759 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0760. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-760 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0761. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-761 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0762. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-762 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0763. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-763 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0764. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-764 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0765. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-765 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0766. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-766 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0767. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-767 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0768. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-768 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0769. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-769 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0770. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-770 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0771. Permissions — Inspect
**Objective:** inspect the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-771 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0772. Ssh — Document
**Objective:** document the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-772 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0773. Cron — Test
**Objective:** test the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-773 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0774. Systemd — Compare
**Objective:** compare the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-774 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0775. Journalctl — Isolate
**Objective:** isolate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-775 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0776. Apt — Record
**Objective:** record the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-776 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0777. Permissions — Validate
**Objective:** validate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-777 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0778. Ssh — Review
**Objective:** review the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-778 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0779. Cron — Restore
**Objective:** restore the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-779 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0780. Systemd — Verify
**Objective:** verify the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-780 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0781. Journalctl — Inspect
**Objective:** inspect the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-781 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0782. Apt — Document
**Objective:** document the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-782 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0783. Permissions — Test
**Objective:** test the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-783 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0784. Ssh — Compare
**Objective:** compare the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-784 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0785. Cron — Isolate
**Objective:** isolate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-785 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0786. Systemd — Record
**Objective:** record the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-786 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0787. Journalctl — Validate
**Objective:** validate the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-787 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0788. Apt — Review
**Objective:** review the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-788 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0789. Permissions — Restore
**Objective:** restore the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-789 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0790. Ssh — Verify
**Objective:** verify the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-790 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0791. Cron — Inspect
**Objective:** inspect the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-791 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0792. Systemd — Document
**Objective:** document the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-792 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0793. Journalctl — Test
**Objective:** test the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-793 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0794. Apt — Compare
**Objective:** compare the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-794 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

## 0795. Permissions — Isolate
**Objective:** isolate the permissions behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of permissions. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If permissions behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-795 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include SSH and cron.

## 0796. Ssh — Record
**Objective:** record the SSH behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of SSH. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If SSH behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-796 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include cron and systemd.

## 0797. Cron — Validate
**Objective:** validate the cron behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of cron. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If cron behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-797 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include systemd and journalctl.

## 0798. Systemd — Review
**Objective:** review the systemd behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of systemd. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If systemd behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-798 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include journalctl and apt.

## 0799. Journalctl — Restore
**Objective:** restore the journalctl behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of journalctl. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If journalctl behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-799 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include apt and permissions.

## 0800. Apt — Verify
**Objective:** verify the apt behavior without assuming the machine is configured as expected.

**Procedure:** Begin with a read-only inspection of apt. Capture the command, timestamp, exit status, and a concise interpretation. Compare the observed result with the intended state. If a change is proposed, state the exact files, services, accounts, or network paths affected before requesting approval.

**Evidence:** A useful record includes the environment, relevant version, input parameters, output summary, and whether the result was verified independently. Avoid reporting a change as successful merely because a command returned zero; check the resulting state.

**Failure handling:** If apt behaves unexpectedly, stop escalating privileges. Preserve logs, identify the smallest reversible next step, and offer rollback instructions. Do not delete evidence or repeatedly retry a potentially destructive operation.

**Angel behavior:** In normal chat, explain the concept and ask clarifying questions. In Sys Chat, propose a dry run first, show the planned operation, request permission for impactful actions, enforce timeouts, and report both successes and limitations.

**Example note:** Case 2-800 should remain understandable months later: what was checked, why it mattered, what changed, and how the result was confirmed. Related concerns include permissions and SSH.

