[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "angel-platform"
version = "1.1.0"
description = "Angel AI and Angel Nexus unified desktop foundation"
requires-python = ">=3.10"
dependencies = []
